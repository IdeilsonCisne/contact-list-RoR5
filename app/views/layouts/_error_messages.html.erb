<% if object.errors.any? %>
  <div class="">
    <div class="alert alert-danger">
      Oops! We find <%= pluralize(object.errors.count, 'error', 'errors') %>
      <ul>
        <% object.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  </div>
<% end %>
